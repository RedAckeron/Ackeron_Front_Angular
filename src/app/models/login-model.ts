export class Login {
        credential : string;
        password : string;
    
        constructor(cred : string, pwd : string){
            this.credential = cred;
            this.password = pwd;
        }
    }